version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/Zerozero-server
permissions:
  - object: /home/ubuntu/Zerozero-server/
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu
